name: Test
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Change Name
        uses: Matuyuhi/github-user-languages-info@v1.0.0
        with:
          who-to-greet: 'World!!!'